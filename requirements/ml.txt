# =============================================================================
# ml.txt — ML / Vision stack (transformers, torch, faiss, opencv)
#
# Usage:
#   -r ../requirements/ml.txt
# =============================================================================

-r base.txt

# ML frameworks
transformers==4.53.0
torch==2.12.0
numpy==1.26.4

# Vector store
faiss-cpu==1.9.0

# Computer vision
opencv-python-headless==4.10.0.84

# Data processing
jq==1.8.0
